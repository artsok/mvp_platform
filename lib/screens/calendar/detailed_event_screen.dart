import 'package:flutter/material.dart';

class DetailedEventScreen extends StatelessWidget {

  static const routeName = '/detailed-event-screen';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: const Text('Detailed event screen'),
      ),
    );
  }
}
